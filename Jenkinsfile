#!groovy

pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "Branch Demo2------Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
        stage('Example sdsdfsfd') {
            steps {
                echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
    }
}
